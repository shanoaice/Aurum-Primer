enum AppRoutes {
	Dashboard = '/dashboard',
	Proxies = '/proxies',
	Rules = '/rules',
	Connections = '/conns',
	Logs = '/logs',
	Settings = '/settings',
	Profiles = '/profiles',
}

export default AppRoutes;
