enum AppRoutes {
	Dashboard = '/dashboard',
	Proxies = '/proxies',
	Rules = '/rules',
	Connections = '/conns',
	Logs = '/logs',
	Settings = '/settings',
	Profiles = '/profiles',
	Setup = '/',
}

export default AppRoutes;
